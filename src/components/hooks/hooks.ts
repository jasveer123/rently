const useLogin = () => {
    const onClikHandler = () => {}

    const onChangeHandler = () => {}

    return {
        state: {},
        action: { onClikHandler, onChangeHandler },
    }
}

export default useLogin
